CNU LLM Bot - Ralph Progress
=============================
Scope: 학과=컴퓨터인공지능학부만, PDF/HWP 제외. 추론=Colab T4 15GB.
환경: 로컬 miniconda Python 3.13.11 (torch CPU만, chromadb/sentence-transformers 미설치 -> dense는 Colab)

[Phase A] 로컬 벡터DB + 검색 검증 - 완료
- embedding/data_loader.py 신규: load_scoped_docs() - all_dedup.json(257건) 우선 로드,
  폴백은 개별 카테고리 파일(학과 denylist 제외). 컴공 department_general만, 다른 학과 제외.
- 검증: 257건 로드, 14 카테고리 (F_department 20=컴공, 인문/사회 학과 없음 -> 스코프 정확).
- BM25 sparse 검색 실데이터 동작 확인 (rank_bm25 설치됨). 샘플 5질문 관련 청크 반환.
  예: "컴퓨터인공지능학부 교수" -> F_department sparse=6.79 강한 매칭.
- dense(bge-m3)는 chromadb 미설치로 graceful skip -> Colab에서 동작 (설계대로).
- 전 모듈 py_compile 통과 (exit 0).
- 알려진 약점: BM25 한국어 토크나이저가 공백분할이라 일부 매칭 거침. dense가 Colab서 보완. (open item)

[Phase B] 콜랩 노트북 정비 - 완료
- notebooks/main.ipynb 셀 5 패치: run_all_crawlers() 재크롤 -> load_scoped_docs() 깃 캐시 로드.
  콜랩서 재크롤 느림/실패위험 제거. 셀 4 마크다운도 갱신.
- notebook JSON 유효 (nbformat=4, 22셀, 전 셀 well-formed).
- 크로스모듈 호출 일관성 확인: answer_questions가 extract_dates, check_rejection,
  build_user_prompt, hybrid retrieve, generate 정확히 호출 (실제 함수명과 일치).
- 단위검증: rejector(empty->no_results, weather->out_of_domain), date_extractor 4패턴 정확
  (오늘 2026-05-21, 5월19일, 내일 2026-05-22, 이번주).

[Phase C] 메타라우팅, CRAG - 사용자 확인 대기
- Phase A/B 완료. Phase C는 사용자 진행 여부 확인 후.

남은 GPU 검증 (Colab에서 사용자):
- bge-m3 임베딩 + Chroma 벡터DB 빌드
- Qwen/EXAONE 7B 로드 + 답변생성
- end-to-end answer_questions

[E2E 검증 결과 - 2026-05-21 추가]
설치 완료: chromadb, sentence-transformers, FlagEmbedding, kiwipiepy. bge-m3 다운.
수정 완료 버그:
- RRF 형태 불일치: _dense_search 결과 평탄화(original_text 키 통일) -> 융합 정상
- BM25 한국어: hybrid_retriever._tokenize() kiwipiepy 적용 -> "졸업학점" 매칭됨
- .gitignore: data/crawled/*.json 추적 허용 -> 콜랩 clone 데이터 받음. push 완료(90d0358)
검색 단독 검증: "졸업 학점"->정답1등, "국가장학금"->정답1등 (kiwi+RRF수정 효과 확인)

[남은 버그 3개 - 수정 완료 2026-05-21]
1. date_filter dense 크래시 -> FIXED
   원인: answer_questions가 valid_until(문자열) 을 Chroma where $gte로 넘김 -> 타입 비교 예외.
   수정: retrieval/hybrid_retriever.py retrieve()가 date_filter를 Chroma where로 안 넘기고
   융합 후 _apply_date_filter()로 후처리. ISO 날짜는 사전식 비교=시간순. valid_until 없는
   문서(학과 위치 등 상시정보)는 통과. 날짜필터 시 검색량 2배로 늘려 후처리 손실 보전.
2. 리랭커 버전 충돌 -> FIXED
   원인: FlagEmbedding FlagReranker가 transformers 비호환(prepare_for_model 없음).
   수정: retrieval/reranker.py를 sentence-transformers CrossEncoder("BAAI/bge-reranker-v2-m3")로
   교체 + 모듈레벨 _cross_encoder 캐시(호출마다 재로드 방지). 로드 실패시 융합순위 폴백.
3. 과잉 거절 -> FIXED
   원인(진짜): rejector가 점수 추출 시 rrf_score를 1순위로 읽음. RRF점수 최대 ~0.033인데
   임계값 0.6 -> 항상 거절. (date_filter 크래시까지 겹쳐 dense 점수도 0이었음)
   수정: generation/rejector.py가 rrf_score를 거절판정에서 제외. dense_score(코사인,0~1) 우선
   임계값 0.35, 없으면 sparse(BM25) 임계값 3.0으로 판정. env로 조정 가능
   (REJECT_DENSE_THRESHOLD, REJECT_SPARSE_THRESHOLD). 평가셋으로 추후 튜닝.

[현재 동작 상태 - 수정 후]
- 검색(하이브리드, kiwi): standalone 검증 OK
- 3개 버그 수정 후 4개 파일 py_compile 통과.

[2026-05-22 검증 완료 + 청킹 + CRAG 진행]
- 데이터 갱신: all_dedup.json 257->325건(공지 35 포함). 백업 all_dedup_backup_2026-05-22.json.
- 청킹 ON: embedding/chunker.py 자체구현(langchain 의존 제거). 325문서->423청크.
  scripts/rebuild_index.py 로 벡터DB 청크단위 재빌드 완료(423).
- 버그3개 검증 7/7 통과(tests/verify_retrieval_only.py):
  s1~s6 통과(dense 0.60~0.74, sparse 8.9~20.8), s7(날씨)만 out_of_domain 거절.
- 핵심 발견: "오늘 날씨" dense=0.452 -> dense임계값 0.35로는 못 거름(키워드필터가 잡음).
  bge-m3 코사인은 무관해도 0.45 바닥. => 점수게이트 한계 확인, CRAG+리랭커 게이트 필요성 입증.
- 데이터 품질 이슈: 생협(G_student_life) 문서 1건 cp949 인코딩 깨짐. 나중에 재크롤.
- 리랭커: rerank_score를 시그모이드 0~1 정규화로 변경(reranker.py). rerank_raw도 저장.
- 다음: tests/calibrate_reranker.py 로 도메인안/밖 리랭커 점수 측정 -> CRAG 밴드 경계 확정
  -> rejector.py를 CRAG(리랭커 점수 밴드)로 교체, answer_questions에 corrective 재검색.

[2026-05-22 크롤링 데이터 품질 개선 (챗봇 본문)]
- 문제 발견: 챗봇인데 본문 없는 카테고리들. 데이터로 진단:
  * K_notices: 35건 전부 제목만(41자). notices.py가 content=title.
  * admin/scholarship/career/academic: '게시물 NNNN'(11~14자) 쓰레기. _fetch_post의
    CSS셀렉터(.board_view_content 등)가 plus.cnu.ac.kr HTML과 안 맞아 제목/본문 둘다 실패.
- 해결(SOTA): trafilatura 본문추출로 통일.
  * crawler_pipeline/body_extractor.py 신규: fetch_html(인코딩 자동보정), extract_main_text,
    fetch_body, fetch_post(본문없으면 None 반환 -> 쓰레기 저장 차단).
  * notices.py: 목록->글 링크(urljoin)->상세 본문 추출. 검증: 41자->평균 697자.
  * admin/career/scholarship/academic: _fetch_post를 공용 fetch_post 위임으로 교체.
    검증: 장학글 14자->544자, 제목/날짜 정상 추출.
- 인코딩 자동복구: crawler_pipeline/text_repair.py (한글 늘 때만 latin1/cp1252->cp949).
  base._make_doc + data_loader 양쪽 적용. scripts/fix_encoding.py로 기존 2건 복구.
- requirements.txt에 trafilatura, kiwipiepy 추가.
- 재크롤 스크립트: recrawl_notices.py(완료, 333건/484청크), recrawl_boards.py(진행중).
- 미적용: 중요공지 우선순위(#6), 날짜필터 강화(#7)는 선택적 가산점으로 보류.

[2026-05-22 CRAG 거절게이트 구현]
- reranker.py: rerank_score를 시그모이드 0~1 정규화(rerank_raw도 저장).
- 보정(calibrate_reranker.py, 도메인안24/밖19): 정답 0.62~0.73 / 부분매칭 0.53~0.61 / 무관 0.50~0.52.
  ※ 0.50으로 낮게 나온 도메인안 질문(재수강/전과/기숙사비용 등)은 실제로 데이터에 답 없는 것 = 거절 정상.
- rejector.py: CRAG 3밴드 도입. 리랭커 점수 우선:
  Correct>=0.62 답변 / Ambiguous 0.53~0.62 답변+단서(거절X) / Incorrect<0.53 거절.
  리랭커 없으면 dense0.35|sparse3.0 폴백. env: REJECT_RERANK_CORRECT/INCORRECT.
  RejectionResult에 grade/caveat 추가. _stale_result 헬퍼.
- answer_questions.py: ambiguous면 답변에 "※ 부분일치, 포털 확인" 단서 추가.
- 단위검증: 0.73/0.62 correct, 0.58/0.53 ambiguous(단서), 0.50 incorrect(거절) OK.
- 추후고려(deferred): Ambiguous 시 데모(Gradio)에서 되묻기(clarifying question). 제출=배치라 보류.
- 밴드 경계는 평가셋 200개 e2e로 최종 보정 예정.

[베이스라인 남은 단계]
- (다음) verify_retrieval_only.py 재실행해 CRAG e2e 확인
- 콜랩 노트북 정비(BM25 init·청킹 셀) + Qwen 7B GPU 실추론 e2e
- 평가: testset 200 -> 파이프라인 -> LLM judge 점수
- 그 다음 GraphRAG (2단계)
