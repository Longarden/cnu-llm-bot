# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
*.so
*.dylib

# 가상환경
venv/
.venv/
env/
.env
*.env

# 데이터 / 크롤링 결과
# data/crawled/*.json 는 깃 캐시로 커밋 (콜랩에서 git clone 후 로드). 의도적으로 추적함.
data/distilled/*.json
data/questions/*.json
data/testset.json
# 로컬 백업/스테이징 (커밋 제외)
data/crawled/*.bak*
data/crawled/*backup*
data/crawled_staging/
data/sample_e2e_*.jsonl
data/sample_answers.jsonl

# 벡터 DB
chroma_db/

# Jupyter
.ipynb_checkpoints/
notebooks/__pycache__/

# OMC
.omc/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 로그
*.log
logs/

# 통합 백업/대용량 산출물 (커밋 제외)
data/crawled/*_bak*
data/content_images/
data/crawled_staging/_*.json

# 모델 바이너리/생성출력 (대용량, 다운로드링크로 제출)
model/*.bin
model/*.safetensors
model/checkpoint-*/
outputs/

# 중간 staging(통합본 all_dedup가 정본)
data/crawled_staging/*.json
