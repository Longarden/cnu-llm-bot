# CNU Campus ChatBot 의존성 (Python 3.10 기준)
# 과제 사양: torch 2.5.1 명시. CUDA 빌드는 별도 인덱스에서 설치 권장.
#   예) pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1   # torch 2.5.1 호환쌍 필수. (Colab 기본 torchvision은 torch2.11용이라
#                       안 맞추면 transformers 임포트가 'torchvision::nms' 에러로 통째 깨짐)

# 분류기 + 임베딩 + 로컬 생성(EXAONE/Qwen)
transformers>=4.46.0,<4.49.0   # 4.49+는 CVE-2025-32434로 torch<2.6에서 pytorch_model.bin 로드를
#                                차단함. bge-m3/bge-reranker는 .bin만 배포 → 차단 직전 4.48.x로 고정해
#                                torch 2.5.1로도 로드. (processing_class도 4.46+라 분류기 학습 호환)
sentence-transformers==3.4.1   # 4.x/5.x는 torchcodec(오디오/비디오 디코더)를 무조건 import →
#                                torch2.5.1과 안 맞아 libtorchcodec 로딩 실패로 ST 통째 깨짐.
#                                텍스트 임베딩만 쓰므로 torchcodec 안 끌어오는 3.x로 고정.
accelerate>=0.30.0
autoawq>=0.2.7.post3
FlagEmbedding>=1.2.0

# 검색(하이브리드: BM25 sparse + dense + RRF) / 벡터 DB
chromadb>=0.5.0
rank_bm25>=0.2.2
kiwipiepy>=0.18.0
scikit-learn>=1.4.0
numpy>=1.26.0

# RAG 청킹/유틸
langchain>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0

# UI / 서빙
gradio>=4.0.0
fastapi>=0.110.0
uvicorn>=0.29.0

# 크롤링(실시간 반영 Task3 및 데이터 수집)
beautifulsoup4>=4.12.0
requests>=2.31.0
trafilatura>=1.8.0
python-dateutil>=2.9.0

# (옵션) 외부 API 백엔드 — 평가는 완전 로컬이라 미사용. 데이터 준비 단계에서만 사용.
anthropic>=0.28.0
google-genai>=0.3.0

# PDF/HWP는 이번 빌드 제외 (pyhwp는 Python2 전용이라 콜랩 설치 깨뜨림). 필요시 별도 추가.
